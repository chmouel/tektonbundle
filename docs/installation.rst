.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Tekton Bundle as PipelineRun, run this command in your terminal:

.. code-block:: console

    $ pip install tektonbundle

This is the preferred method to install Tekton Bundle as PipelineRun, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Tekton Bundle as PipelineRun can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/chmouel/tektonbundle

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/chmouel/tektonbundle/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/chmouel/tektonbundle
.. _tarball: https://github.com/chmouel/tektonbundle/tarball/master
